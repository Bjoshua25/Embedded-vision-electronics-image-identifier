# Embedded-vision-electronics-image-identifier
A project that implements TinyML for classification of electronics component images. This TinyML model is being deployed into a microcontroller  (OPENMV). A Dense Neural Networks was utilized in training the image data.
